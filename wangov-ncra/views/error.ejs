<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - NCRA Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.js"></script>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">
    <div class="max-w-md w-full bg-white rounded-lg shadow-lg p-8 text-center">
        <div class="mb-6">
            <div class="mx-auto h-16 w-16 bg-red-100 rounded-full flex items-center justify-center">
                <i data-lucide="alert-circle" class="h-8 w-8 text-red-600"></i>
            </div>
        </div>
        
        <h1 class="text-2xl font-bold text-gray-900 mb-4">
            <% if (typeof error !== 'undefined' && error.status) { %>
                Error <%= error.status %>
            <% } else { %>
                Something went wrong
            <% } %>
        </h1>
        
        <p class="text-gray-600 mb-6">
            <% if (typeof message !== 'undefined') { %>
                <%= message %>
            <% } else { %>
                An unexpected error occurred. Please try again.
            <% } %>
        </p>
        
        <div class="space-y-3">
            <a href="/dashboard" class="block w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 transition-colors">
                Return to Dashboard
            </a>
            <a href="/auth/login" class="block w-full bg-gray-200 text-gray-800 py-2 px-4 rounded-md hover:bg-gray-300 transition-colors">
                Go to Login
            </a>
        </div>
        
        <% if (typeof error !== 'undefined' && error.stack && process.env.NODE_ENV === 'development') { %>
        <details class="mt-6 text-left">
            <summary class="cursor-pointer text-sm text-gray-500 hover:text-gray-700">
                Debug Information
            </summary>
            <pre class="mt-2 p-3 bg-gray-100 rounded text-xs overflow-auto"><%= error.stack %></pre>
        </details>
        <% } %>
    </div>

    <script>
        lucide.createIcons();
    </script>
</body>
</html>
